# Copy this to .env (local dev) and set your values
# These are safe client-side env vars (prefixed with VITE_)
VITE_API_BASE=/ODOverseasPortalAPI/api
VITE_URL_DIR=/ODOverseasPortal

# DO NOT put secrets here. Twilio Account SID/Auth Token must never be in client code.
# Obtain a Voice Access Token from your backend endpoint and use that in the browser.
